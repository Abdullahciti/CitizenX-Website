const handleDynamic = () => {
    // document.querySelector(".bg-white").style = "display: none;"
    console.log("hello")
    // if (window.scrollY >= e.target.offsetHeight) {
        // document.querySelector(".prog .w-50").style = `width: ${document.querySelectorAll(".prog .bg-red").forEach((span) => span.dataset.width)}% !important;`
    // }

};

export default handleDynamic;
